# Codex Task: RA2IniEditor.IDE Icon-2 Icon Concept Sheets

## 0. Current Baseline

Icon-1 has been completed.

Reported state:

```text
Docs/IconStyleGuide.md created.
No source / XAML / ViewModel / tests / project files changed.
No icon resources generated or replaced.
No command semantics changed.
Tests: 1433 passed.
IdeOnly package: passed, packaged file count 772.
```

Icon-1 defines:

```text
modern IDE style
monoline / outline-first
16x16 readability
low visual noise
theme brush / DynamicResource future strategy
semantic naming
WPF vector resource preference
image2 is concept-only, not runtime asset
```

Next phase:

```text
Icon-2: Icon Concept Sheets
```

This phase is a concept/design phase.

Do not modify runtime source or XAML in this phase.

---

## 1. Goal

Create 2-3 visual concept directions for RA2IniEditor.IDE icons before committing to WPF vector resources.

Concept sheets are for visual review only.

They should help decide:

```text
1. visual style
2. stroke weight
3. shape language
4. density at small sizes
5. how RA2IniEditor-specific concepts are represented
```

---

## 2. Hard Boundaries

Do not modify:

```text
ShellWindow.xaml
ShellWindow.xaml.cs
Themes/ShellTheme.xaml
ViewModels
tests
project files
solution files
Field Registry JSON
legacy files
```

Do not:

```text
replace Icon* placeholder resources
add SVG / PNG / DrawingImage runtime resources
change toolbar state
change command handlers
change menu entries
```

This phase only produces concept assets and review documentation.

---

## 3. Required Input

Read:

```text
Docs/IconToolbarInventory.md
Docs/IconToolbarCommandContract.md
Docs/IconStyleGuide.md
```

Use them to define icon list and visual style constraints.

---

## 4. Concept Directions

Generate or prepare prompts for three directions:

### Direction A: Monoline IDE

```text
outline-first
single stroke weight
minimal
best for toolbar 16x16
```

### Direction B: Filled Minimal

```text
simple filled silhouettes
higher contrast
usable for status/actions
```

### Direction C: Hybrid Line + Accent

```text
outline icons with small accent details
warning/error/success use semantic color
AI icon may use accent
```

---

## 5. Required Icon List

Each concept sheet should include at least:

### Main Toolbar

```text
Open Project Folder
Save
Search
Issues
Field Registry
Project Explorer
AI Assistant
```

### Editing / Contextual

```text
Undo
Redo
Revert
Edit Mode
```

### Field Registry

```text
Field Registry Center
Learn Fields
New Field
Edit Field
Import Preview
Rollback
Cleanup
Open Folder
Warning
```

### AI Assistant

```text
Send
Cancel
Copy Message
Copy Code Block
Clear Chat
Model Selector
Markdown
Code Block
Context
```

### Status

```text
Info
Warning
Error
Success
Pending
Disabled
```

---

## 6. image2 Prompt Rules

Each image2 prompt should request:

```text
icon sheet
modern desktop IDE style
no text labels
consistent line weight
transparent or very light neutral background
grid layout
simple vector-like symbols
high readability at 16x16
no 3D
no cartoon style
no decorative RA2 art
```

Important:

```text
Do not treat generated PNG as final runtime asset.
It is a visual concept reference only.
```

---

## 7. Output Required

Create or update:

```text
Docs/IconConceptReview.md
```

Suggested structure:

```markdown
# Icon Concept Review

## 1. Scope

## 2. Concept Directions

## 3. Prompt Texts

## 4. Generated Concept Assets

| Direction | Asset Path | Notes | Strengths | Risks |

## 5. Review Checklist

## 6. Recommended Direction

## 7. Next Implementation Step
```

If actual image files are generated outside Codex, record their filenames / paths only after the user provides them.

If no images are generated by Codex, prepare prompt text and leave asset paths as pending.

---

## 8. Review Criteria

Evaluate each concept by:

```text
1. readability at 16x16
2. consistency of stroke / fill
3. distinction between similar actions
4. dark-theme compatibility
5. semantic clarity
6. WPF vector conversion feasibility
7. no excessive detail
8. fits RA2IniEditor.IDE professional tone
```

---

## 9. Future Phases

After user selects a direction:

```text
Icon-3: WPF vector icon resource contract
Icon-4: Main toolbar icon resource implementation
Icon-5: AI / Field Registry / Issues icon rollout
```

---

## 10. Validation Commands

Documentation/concept task:

```powershell
dotnet test .\RA2IniEditor.Tests\RA2IniEditor.Tests.csproj -c Debug --no-build
powershell -ExecutionPolicy Bypass -File .\tools\package-source-clean.ps1 -Profile IdeOnly
```

If no project files are modified except docs, build is not required unless prior output is missing.

---

## 11. Final Report Format

Report:

```text
1. Phase completed: Icon-2.
2. Files changed.
3. Concept directions prepared.
4. image2 prompts prepared or concept assets recorded.
5. Recommended visual direction, if decided.
6. Commands run.
7. Test/package result.
8. Confirmation no source/XAML/icon runtime resources changed.
9. Recommended next phase.
```
