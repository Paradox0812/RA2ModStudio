# Documentation Archive

This directory preserves superseded accumulated status snapshots. These files are
historical evidence, not current product requirements or active-stage authority.

Use `Docs/README.md` as the current documentation entry point.
